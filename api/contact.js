export default async function handler(req,res){if(req.method!=='POST') return res.status(405).json({error:'Método no permitido'});const {name,email,phone,type,message,privacy}=req.body||{};if(!name||!email||!phone||!message||!privacy)return res.status(400).json({error:'Completa los campos obligatorios.'});const endpoint=process.env.CONTACT_WEBHOOK_URL;if(!endpoint)return res.status(503).json({error:'El formulario aún no tiene configurado el destino de envío.'});try{const r=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,phone,type,message,source:'solamente-cree-web'})});if(!r.ok)throw new Error('Webhook error');return res.status(200).json({ok:true})}catch{return res.status(502).json({error:'No se pudo entregar el mensaje.'})}}